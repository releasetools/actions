{
  "name": "@releasetools/actions",
  "version": "0.0.1",
  "private": true,
  "description": "GitHub Actions for release pipelines: signed-push and friends",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/releasetools/actions.git"
  },
  "scripts": {
    "build:signed-push": "ncc build signed-push/src/index.ts -o signed-push/dist --license licenses.txt --minify",
    "build:plugin-release": "ncc build plugin-release/src/index.ts -o plugin-release/dist --license licenses.txt --minify",
    "build:plugin-release-cli": "ncc build plugin-release/src/bin.ts -o plugin-release/dist-cli --license licenses.txt --minify",
    "build": "npm run build:signed-push && npm run build:plugin-release && npm run build:plugin-release-cli",
    "package:npm": "npm run build:plugin-release-cli && node scripts/package-npm.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@vercel/ncc": "^0.45.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  },
  "overrides": {
    "undici": "^7.16.0"
  }
}
