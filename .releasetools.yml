# What this repository holds, read by every releasetools tool.
# The actions release together on one version, so the repository is one
# project and each action's directory is not.
projects:
  - path: ./
    manifest: package.json
    changelog: CHANGELOG.md
